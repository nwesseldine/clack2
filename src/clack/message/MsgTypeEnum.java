package clack.message;

/**
 * Enum for tracking Message types. Includes: "List Users", "Logout", "Text", "Login", "Option", "File", "Help".
 */
public enum MsgTypeEnum
{
    LISTUSERS,
    LOGOUT,
    TEXT,
    LOGIN,
    OPTION,
    FILE,
    HELP
}
